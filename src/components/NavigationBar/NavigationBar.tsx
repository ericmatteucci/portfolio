import React from 'react';
import './navigationBar.scss';

type NavigationBarPropTypes = {};

class NavigationBar extends React.Component<NavigationBarPropTypes> {
  render() {
    return (
      <div className="navigationBarMain">
      </div>
    );
  }
};

export default NavigationBar;
